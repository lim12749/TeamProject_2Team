using UnityEngine;

public class testcodede : MonoBehaviour
{
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {
            Debug.Log("집가고싶다.");
        }
    }
}
