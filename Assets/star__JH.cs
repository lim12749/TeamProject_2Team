using UnityEngine;

public class star_JH : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("*");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
